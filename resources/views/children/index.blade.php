@extends('layouts.app')
@section('title', 'Children')
@section('content')
<div x-data="{ search: '' }">
    <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><x-page-header title="Children" subtitle="Manage your daycare roster and classroom assignments."/><a href="{{ route('children.create') }}" class="inline-flex w-fit items-center gap-2 rounded-xl bg-indigo-600 px-4 py-3 text-sm font-semibold text-white shadow-lg shadow-indigo-500/25 hover:bg-indigo-700">+ Add Child</a></div>
    @if(session('success'))<div class="mt-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800">{{ session('success') }}</div>@endif
    @php($nextDirection = fn ($column) => $sort === $column && $direction === 'asc' ? 'desc' : 'asc')
    @php($sortUrl = fn ($column) => route('children.index', ['sort' => $column, 'direction' => $nextDirection($column)]))
    @php($arrow = fn ($column) => $sort === $column ? ($direction === 'asc' ? ' ↑' : ' ↓') : '')
    <section class="glass-card mt-7 overflow-hidden rounded-2xl"><div class="border-b border-slate-100 p-4 dark:border-white/10"><input x-model="search" class="w-full rounded-xl border-0 bg-slate-100 px-4 py-3 text-sm ring-1 ring-slate-200 focus:ring-2 focus:ring-indigo-500 dark:bg-slate-800 dark:ring-white/10" placeholder="Search this page by child name or classroom..."></div><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500 dark:bg-slate-800/60"><tr><th class="px-6 py-4 font-semibold"><a href="{{ $sortUrl('lan') }}" class="hover:text-indigo-600">LAN{{ $arrow('lan') }}</a></th><th class="px-6 py-4 font-semibold"><a href="{{ $sortUrl('last_name') }}" class="hover:text-indigo-600">Child{{ $arrow('last_name') }}</a></th><th class="px-6 py-4 font-semibold"><a href="{{ $sortUrl('age') }}" class="hover:text-indigo-600">Age{{ $arrow('age') }}</a></th><th class="px-6 py-4 font-semibold"><a href="{{ $sortUrl('classroom') }}" class="hover:text-indigo-600">Classroom{{ $arrow('classroom') }}</a></th><th class="px-6 py-4 font-semibold"><a href="{{ $sortUrl('status') }}" class="hover:text-indigo-600">Status{{ $arrow('status') }}</a></th><th class="px-6 py-4 font-semibold">Actions</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/10">@forelse($children as $child)<tr x-show="@js(strtolower($child->first_name.' '.$child->last_name.' '.$child->classroom.' '.$child->lan)).includes(search.toLowerCase())" class="hover:bg-slate-50/80 dark:hover:bg-white/5"><td class="px-6 py-4 text-slate-500">{{ $child->lan }}</td><td class="px-6 py-4"><div class="flex items-center gap-3"><span class="grid h-9 w-9 place-items-center rounded-full bg-indigo-100 font-bold text-indigo-700">{{ strtoupper(substr($child->first_name,0,1)) }}</span><span class="font-semibold">{{ $child->first_name }} {{ $child->last_name }}</span></div></td><td class="px-6 py-4 text-slate-500">{{ $child->age ?? '—' }}</td><td class="px-6 py-4">{{ $child->classroom }}</td><td class="px-6 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $child->status === 'Active' ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-600' }}">{{ $child->status }}</span></td><td class="px-6 py-4"><a href="{{ route('children.edit', $child) }}" class="font-semibold text-indigo-600 hover:text-indigo-800">Edit</a></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">No children have been added yet. Use “Add Child” to create your first record.</td></tr>@endforelse</tbody></table></div>@if($children->hasPages())<div class="flex items-center justify-between border-t border-slate-100 px-5 py-4 text-sm dark:border-white/10"><span class="text-slate-500">Showing {{ $children->firstItem() }}–{{ $children->lastItem() }} of {{ $children->total() }}</span><div class="flex gap-2">@if($children->onFirstPage())<span class="rounded-lg px-3 py-2 text-slate-400">Previous</span>@else<a href="{{ $children->previousPageUrl() }}" class="rounded-lg bg-slate-100 px-3 py-2 font-semibold hover:bg-slate-200 dark:bg-slate-800">Previous</a>@endif @if($children->hasMorePages())<a href="{{ $children->nextPageUrl() }}" class="rounded-lg bg-indigo-600 px-3 py-2 font-semibold text-white hover:bg-indigo-700">Next</a>@else<span class="rounded-lg px-3 py-2 text-slate-400">Next</span>@endif</div></div>@endif</section>
</div>
@endsection
